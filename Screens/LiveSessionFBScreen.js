import * as React from 'react';
import { View, Text, Linking} from 'react-native';

export function LiveSessionFBScreen() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>Live Session FB Screen!</Text>
      </View>
    );
  }